function OnStart()
	-- Called once when the script is attached to an entity 
end

function OnUpdate(deltaTime)
	-- Called every frame while the entity exists
end

function OnDestroy()
	-- Called when the script is removed or entity is destroyed
end